#!/bin/bash

#set -x

### GENERAL
MAMEBASE=${HOME}/games/emulator-mame
PROFILES=./profiles
MAMELOCAL=/mnt/mame-media
MEDIALOCAL=${MAMELOCAL}/media
NVRAMPATH=${MEDIALOCAL}/nvram
FLOPPATH=${MEDIALOCAL}/floppies
HDDPATH=${MEDIALOCAL}/hdd
CDPATH=${MEDIALOCAL}/cdrom


### MACHINE
RAMSIZE=${RAMSIZE:-64M}
VGA=${VGA:-svga_s3}
SND=${SND:-sblaster_16}
MODEM=127.0.0.1
PORT=25232
NETWORK=${NETWORK:-ne2000}
SERIAL=${SERIAL:-}
